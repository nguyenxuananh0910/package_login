import 'package:flutter_test/flutter_test.dart';

import 'package:package_login/package_login.dart';

void main() {
  test('adds one to input values', () {
    final calculator = CoreUserPackage();
  });
}
